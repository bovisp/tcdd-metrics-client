import Vue from 'vue'

export const CreateBadgeLanguage = Vue.component('createBadgeLanguage', require('./Create.vue').default)
export const BadgeLanguages = Vue.component('badgeLanguages', require('./Index.vue').default)
