import Vue from 'vue'

export const CreateWebinarAttendance = Vue.component('createWebinarAttendance', require('./Create.vue').default)
export const WebinarAttendance = Vue.component('webinarAttendance', require('./Index.vue').default)
