import Vue from 'vue'

export const CreateCourseLanguage = Vue.component('createCourseLanguage', require('./Create.vue').default)
export const CourseLanguages = Vue.component('courseLanguages', require('./Index.vue').default)
