import { CourseMetadata } from '../components'

export default [
  {
    path: '/course-metadata',
    component: CourseMetadata,
    name: 'courseMetadata',
    meta: {
      needsAuth: false
    }
  }
]
